import './index.scss';

const Pharmacy = () => (
  <div className="pharmacy-view">
    pharmacy's name
    address
    primary phone number
    pharmacy hour (if there)
  </div>
);

export default Pharmacy;
